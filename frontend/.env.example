# Seeds the in-app settings store on first run only. After that, the operator
# manages API base / service key / tenant id from the Settings page (localStorage).
VITE_API_BASE=http://localhost:4020/api/v1
